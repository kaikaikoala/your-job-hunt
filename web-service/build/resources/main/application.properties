spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/jobhunt}
spring.datasource.username=${DB_USER:jobhunt}
spring.datasource.password=${DB_PASSWORD:jobhunt}
spring.jpa.hibernate.ddl-auto=validate
spring.flyway.locations=filesystem:../database/migrations
spring.flyway.baseline-on-migrate=true
